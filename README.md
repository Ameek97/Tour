# Natours

Tour booking app (Express API + Vite React UI).

## Scripts

- `npm run dev` / `npm start` — API (`PORT` or 3000)
- `npm run build:frontend` — production UI into `frontend/dist`

See `PRODUCTION.md` and `config.env.example`.
