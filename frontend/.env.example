# Leave empty so the browser calls same-origin `/api`.
# Local Vite proxies `/api` to http://localhost:3000.
# Production: leave empty when Express serves frontend/dist.
# Split hosts only: VITE_API_URL=https://api.example.com
VITE_API_URL=
